<ul class="list-group">
  <%= link "Add new post", to: post_path(@conn, :new), class: "list-group-item active" %>
  <%= for post <- @posts do %>
    <%= link post.title, to: post_path(@conn, :show, post), class: "list-group-item" %>
  <% end %>
</ul>
